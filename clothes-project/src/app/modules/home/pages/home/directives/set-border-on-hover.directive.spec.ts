import { SetBorderOnHoverDirective } from './set-border-on-hover.directive';

describe('SetBorderOnHoverDirective', () => {
  it('should create an instance', () => {
    const directive = new SetBorderOnHoverDirective();
    expect(directive).toBeTruthy();
  });
});
